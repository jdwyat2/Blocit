<h1>Advertisements</h1>
<%= @advertisements.each do |advert| %>
    <div class="media">
        <div class="media-body">
            <h4 class="media-heading">
                <%= link_to advert.title, post %>
            </h4>
        </div>
    </div>

<% end %>
